<!--Sandeep Reji-->
<!--Index of Created Locations-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
     integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

    <title>Blog</title>
</head>
<body>
    <div class="container"> <!--Button to create a new location-->
    <h1 class="mb-4"></h1>Location Generator</h1>
    <a href = "/articles/new" class="btn btn-success">Click here to Generate New Location</a>

    <% articles.forEach(article => { %>  <!--Will print out the title and description of each-->
        <div class="card mt-4">
          <div class="card-body">
            <h4 class="card-title"><%= article.title %></h4>
            <div class="card-subtitle text-muted mb-2">
                <%= article.createdAt.toLocaleDateString() %>
                </div>
                <div class="card-text mb-2"><%=
                    article.description %></div>
                    <a href="articles/<%= article.slug %>"class="btn 
                        btn-primary">Read More</a>                      <!--Will go more indepth for each location, including the climate, locale type, and amenities-->

                        
                        <form action="/articles/<%= article.id %>?_method=DELETE" 
                            method="POST" class="d-inline">                         <!--Delete button to delete an individual location-->
                        <button type="submit" class="btn btn-danger">Delete</button>

            
        </div>
        </div>
      <% }) %>
  </body>
  </html>